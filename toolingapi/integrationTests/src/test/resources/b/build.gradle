apply plugin: 'java'

repositories {
    mavenLocal() // helps with offline integration testing (assuming you've got a populated maven central repo)
    mavenCentral()
}

dependencies {
    compile 'com.fasterxml.jackson.dataformat:jackson-dataformat-xml:2.4.1'
}